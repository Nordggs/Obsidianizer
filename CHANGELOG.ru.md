# Obsidianizer

## [0.6.4] — 05.09.2026

### Добавлено
- **Индикатор новой версии в приложении** — при запуске программа тихо
  проверяет GitHub Releases (через 2 секунды после старта, таймаут 8 с,
  оффлайн деградирует безошибочно) и при наличии более новой версии
  показывает пульсирующую зелёную точку рядом с версией в статус-баре.
  Клик открывает окно с текущей/новой версией и кнопкой перехода на
  страницу релиза в системном браузере. Без автообновления; Draft
  игнорируются, Pre-release считаются валидным релизом (все наши релизы —
  Pre-release). Стилистика огонька общая 1:1 с Project AI Base. Строки
  локализованы (RU/EN).

## [0.6.3] — 05.09.2026

### Изменено
- **Актуальность карточек по содержимому** — fingerprint карточки
  (`obsidianizer_hash`) теперь считается по СОДЕРЖИМОМУ файлов (sha1), а не
  по `mtime`. Облачная синхронизация (Dropbox и т.п.) и фоновые процессы
  трогают mtime, не меняя ни байта, — из-за этого карточки залипали в
  `stale` (`status=stale` при `changed=0`). `mtime` остался в метаданных
  только для отображения (колонки «Изменён» / «Updated») и на решение
  «устарела ли карточка» больше не влияет. Скрытый манифест хранит
  `{path: {size, hash}}`, а `card_diff` определяет `changed` по содержимому:
  правки того же размера (AAAA→BBBB) ловятся, mtime-джиттер игнорируется.
  Одноразовый эффект: первый прогон пересобирает все карточки (смена формулы
  fingerprint); скан читает содержимое файлов и занимает чуть больше времени.

### Исправлено
- **Кнопка Up у карточек первого уровня** — карточка-прямая-дочка скан-корня
  получала голый линк `[[..|Up]]`, указывающий на родительскую ПАПКУ, которую
  Obsidian открыть не может (диалог «file already exists» при клике). Теперь
  линк ведёт на карточку корня (`../<имя корня>`), как и на всех нижних
  уровнях.

## [0.6.2] — 02.09.2026

### Изменено
- **Пользовательские поля заметок/карточек стали универсальными** —
  «клиент» → «проект», «дизайнер» → «контакт» (privacy-гейт перед публичным
  релизом). Заметки из v0.5.x продолжают работать: старые ключи
  перечитываются через маппинг, карточка показывает новые имена полей с теми
  же значениями, миграция со старой карточки пишет в заметки новые имена.

## [0.6.1] — 02.09.2026

### Изменено
- **Структурные события (этап 2 i18n)** — `Event` теперь несёт машиночитаемый
  payload `data` (счётчики, флаги состояния). UI читает `ev.data` вместо
  парсинга локализованного текста, поэтому прогресс и статусы отображаются
  одинаково в RU и EN.
- **Локализованы сообщения бэкенда** — ошибки и видимые в UI строки логов
  pipeline, постобработки, тем, LLM-клиента и защитника путей следуют
  активному языку. Markdown-контент (карточки, темы, промпты) не затронут.

## [0.6.0] — 02.09.2026

### Добавлено
- **Локализация интерфейса RU/EN** (этап 1 i18n) — все кнопки, подписи,
  подсказки, плейсхолдеры, статусы и ошибки переключаются на лету тумблером
  RU|EN в футере. Выбор сохраняется в `config.yml`; при первой установке
  язык определяется по системной локали.
- Новая i18n-инфраструктура: `web/i18n.js` (JS-словари + `t()` +
  `applyI18n()`) и `i18n.py` (`tr()`) для сообщений бэкенда.

### Зафиксированные принципы
- **Язык UI ≠ язык данных**: Markdown-карточки, LLM-промпты и Templater-
  шаблон остаются на русском независимо от языка интерфейса.
- Развязка сообщений бэкенда (структурные события) — в следующем релизе.

## [0.5.7] — 02.09.2026

### Добавлено
- **Кнопка «Извлечь шаблоны в Obsidian»** (Справка → Integration) — копирует
  файлы ручной настройки Templater (`Obsidianizer Update.md`,
  `shell-commands.json`) в выбранную пользователем папку. Существующие файлы
  не перезаписываются молча: при конфликте имён показывается предупреждение,
  повторный клик подтверждает перезапись (паттерн Repair).
- Установщик дополнительно кладёт `integration/` в
  `%LOCALAPPDATA%\Obsidianizer\integration\` — файлы доступны без поиска
  в Program Files. Portable-версия по-прежнему использует папку рядом с exe.

## [0.5.6] — 30.08.2026

### Изменено
- **Кнопка Obsidian Integration** теперь открывает окно справки с инструкциями
  по настройке интеграции вместо проверки vault. Проверка и установка
  интеграции доступны во вкладке Integration окна справки.

## [0.5.5] — 30.08.2026

### Исправлено
- **PermissionError при запуске из Program Files** — лог и конфиг теперь
  пишутся в `%LOCALAPPDATA%\Obsidianizer\` вместо папки установки.
  Деинсталлятор тоже очищает эту папку.

## [0.5.4] — 30.08.2026

### Безопасность
- **Зачищены все личные данные** из исходного кода, документации и
  Git-истории: фикстуры тестов теперь используют generic-имена (А. В., Д. З.),
  вымышленные телефоны (`+7 (999) 123-45-67`) и шаблонные пути
  (`PROJECT/OBSIDIAN/Objects`, `D:\Path\To\...`). Git-история перезаписана
  через `git filter-repo`; старые теги (v0.3.0–v0.5.3) удалены. Первый
  релиз, готовый к публикации.

## [0.5.3] — 29.08.2026

### Исправлено
- **Вкладка Integration: кнопка «Обзор» теперь работает** — `api` оказывался
  `null` до события `pywebviewready`; та же причина влияла на кнопки
  «Проверить» и «Установить» (#2, #3).
- **Таблица сканирования: ✓ вместо ?** для статуса «ок» (#1).
- **Vault интеграции изолирован** от рабочей папки Obsidianize — новая
  настройка `integration_vault` не даёт вкладкам перезаписывать пути
  друг друга.

### Добавлено
- **Папка `integration/`** для ручной установки Templater / Shell Commands:
  инструкция (`Obsidianizer Update.md`), Templater-шаблон с плейсхолдером
  `{{CLI_PATH}}` и конфиг для плагина Shell Commands в реальном формате (#4).
- Окно справки: вкладка Integration теперь сообщает о возможности ручной
  установки.

## [0.5.2] — 28.08.2026

### Релиз документации — переосмысление продукта
- README (RU+EN) полностью перестроен вокруг того, чем приложение является:
  **Obsidianizer — локальный инструмент для подготовки проектных материалов к
  работе в Obsidian**, три инструмента в одном приложении:
  1. **Obsidianize** (основной) — обычные проектные папки → карточки,
     навигация, галереи;
  2. **Chat Processing** — экспортированные чаты **и другие
     Markdown-материалы** (частый сценарий, но не обязательный источник) — с
     отдельным разделом про **AI Chat Exporter** и ссылкой на
     проект-компаньон (github.com/Nordggs/Project_AI_Base) и цепочкой
     ACE → Markdown → Chat Processing → Obsidian;
  3. **AI-анализ** — поиск/обзор/суммирование в **любой указанной папке**
     (результат чатов, проектные папки, весь vault), а не только вывод
     чат-обработки.
- Формулировка «локальный препроцессор» ушла из интро (осталась в
  архитектурной документации); раздел интеграции с Obsidian переписан как
  пошаговый сценарий (установка → указание vault → Install → проверка/
  назначение хоткея → Alt+3) с явной границей ответственности:
  **Obsidianizer никогда не редактирует hotkeys.json вашего vault** —
  `Alt+3` лишь пример, клавишу проверяет и назначает пользователь в
  Obsidian.
- Тексты плавающего окна справки и `obsidianizer-refresh.md` синхронизированы
  с тем же позиционированием; добавлен шаг «проверьте хоткей».
- `AGENTS.md`: позиционирование продукта зафиксировано как правило (порядок
  и формулировки для документации, подписей интерфейса и заметок релизов).

## [0.5.1] — 28.08.2026

### Исправлено
- **Упаковка: EXE падал при первом запуске из скачанного архива**
  (`RuntimeError: Failed to resolve Python.Runtime.Loader.Initialize`).
  Причина: Windows Mark-of-the-Web — архивы, скачанные с GitHub, несут
  метку зоны на каждом распакованном файле, и хост .NET Framework
  отказывается грузить заблокированные сборки, поэтому WinForms-бэкенд
  pywebview не стартовал. Исправлено поставкой `Obsidianizer.exe.config`
  (с `loadFromRemoteSources enabled`) **рядом с EXE**:
  - `build-release.bat` автоматизирует сборку и всегда копирует конфиг;
  - `Obsidianizer.iss` доставляет его и для инсталлятора;
  - проверено MOTW-симуляцией (метки зоны проставлены на все файлы
    собранной сборки — приложение стартует).
- Примечание к `v0.5.0`: эта сборка подвержена багу — распаковка и запуск
  из скачанного архива приводят к падению; используйте **v0.5.1** или
  выполните `Unblock-File` на распакованной папке.

## [0.5.0] — 28.08.2026

### Добавлено
- **Интеграция с Obsidian** — настройка хоткея одной кнопкой:
  - кнопка 🔗 «Obsidian Integration» на вкладке Obsidianize: копирует
    шаблон обновления в папку Templater вашего vault, **автоматически
    подставляя реальный путь к Obsidianizer** (без ручного прописывания
    путей и PATH);
  - плавающее окно справки получило вкладку **🔗 Integration** с живым
    чек-листом статуса — vault ✓/✗, Templater ✓/✗, шаблон ✓/✗, CLI ✓/✗ —
    и кнопкой «Установить / Починить» (уже установленный шаблон не
    перезаписывается без явного повторного нажатия);
  - отсутствие Templater — это подсказка с инструкцией, а не ошибка.
- **Собранный EXE работает и как CLI**: `Obsidianizer.exe folders …`
  вызывает тот же движок, что и `obsidianizer-cli.bat`, — установленной
  сборке не нужны bat-файлы.
- `src/obsidianizer/integration.py`: определение пути CLI (frozen vs
  исходники), поиск папки Templater (читает `data.json` плагина, fallback
  на `<vault>/templates`), атомарная установка шаблона с JS-экранированием
  путей.

Фич-релиз: линейка 0.4.x (восстановление, исправления, документация,
рестайлинг) завершена и стабильна.

## [0.4.5] — 28.08.2026

### Исправлено
- Заставка рендерилась в родном размере 1408×768 — больше стартового окна:
  CSS-блок `#splash` случайно отсутствовал с v0.4.1 (div и JS-растворение
  были на месте, а стили — нет). Теперь таблица стилей заставки на месте,
  картинка ограничена `min(70%, 900px)` по ширине и 70% по высоте — по
  центру на фоне `#0b0f17`, заметно внутри окна, в духе AI Chat Exporter.

## [0.4.4] — 28.08.2026

### Изменено
- Стартовый размер окна приведён к **AI Chat Exporter** (1300×750, фон
  `#0b0f17`) — заставка теперь рендерится одинаково в обеих программах, а
  не вылезает за пределы меньшего окна.

### Добавлено
- Справка «?» стала **отдельным плавающим окном** (как окно AI-чата): её
  можно тащить куда угодно — в том числе за пределы главного окна — и она
  остаётся поверх. Кнопка «?» передаёт активную вкладку; в окне справки
  есть собственный переключатель (Obsidianize / Чат-обработка /
  AI-анализ), стартовая секция берётся из hash URL, а при уже открытом
  окне срабатывает push `showHelpTab`. Заменила внутриоконную модалку,
  которая физически не могла покинуть границы приложения.

## [0.4.3] — 28.08.2026

### Исправлено
- Разделитель размера таблицы карточек сломался после v0.4.2 (вкладка стала
  flex-колонкой со своим скроллом, и scroll anchoring Chromium сдвигал
  содержимое во время перетаскивания). Страница вернулась к общему скроллу
  окна (настройки, таблица и лог прокручиваются вместе, как и было
  запрошено), а разделитель теперь считает высоту по абсолютной позиции
  курсора — нечувствителен к скроллу.
- Панель статуса (версия + сводка скана) закреплена через
  `position: sticky; bottom: 0` — видна на каждой вкладке при прокрутке
  всего окна.

### Изменено
- Полосы прокрутки перекрашены в палитру AI Chat Exporter (тёмный жёлоб
  `#0b0f17`, бегунок `#2a3444` → `#4a5568` при наведении) — действует на
  все прокручиваемые области: таблицу карточек, журнал, модальные окна.
- Выпадающий список шаблона (`github/classic`) тоже тёмный
  (`select` + `option`) — больше не белый системный.

## [0.4.2] — 28.08.2026

### Изменено
- Панель статуса теперь **закреплена внизу окна** на всех вкладках
  (содержимое вкладки прокручивается внутри своей области — как в макете
  AI Chat Exporter): сводка скана («проверено N · требуют обновления: M»)
  больше не уезжает за экран.
- Значок версии переехал в **левый нижний угол** той же панели
  (11px, `#4a5568`, выделяемый — точный стиль AI Chat Exporter) и всегда
  виден на всех вкладках.
- Подпись в шапке приведена к точному стилю AI Chat Exporter (10px,
  прозрачность 0.6, капс, без разрядки).

## [0.4.1] — 28.08.2026

### Изменено
- Полный визуальный рестайлинг под **AI Chat Exporter**: палитра переехала с
  нейтральной VS-темы на тёмно-синюю ACE (`#0b0f17` фон, градиентные панели
  `#121826→#0e1420` с радиусом 14px, границы `#1f2a3a`), акцент теперь
  `#2f6bff`, статусные цвета `#0ea56a`/`#f0b400`/`#ff5555`, шрифт — Inter
  (fallback system-ui). Кнопки, поля ввода, чекбоксы, выпадающие списки,
  списки тем/чатов, модальные окна (радиус 12px, граница `#2a3345`) и ~30
  хардкод-цветов перекрашены в ту же палитру.
- Шапка стала топбаром в стиле AI Chat Exporter: `Obsidianizer` + подпись
  `LOCAL PROJECT CARDS`, версия перенесена к правому краю.

### Добавлено
- Splash-экран при запуске GUI — `Obsidianizer Заставка.png` (на весь экран,
  растворяется через ~1.8 с), как в AI Chat Exporter.
- `Obsidianizer.ico` в корне репозитория (мультиразмерный, собран из
  `Obsidianizer ярлык.png`) для значка ярлыка Windows, плюс `<link
  rel="icon">` и `icon.png` в web-ресурсах; в README описано, как назначить
  значок ярлыку.

## [0.4.0] — 28.08.2026

### Добавлено
- Контекстная справка: кнопка «?» справа от панели вкладок открывает
  объяснение простым языком для **активной** вкладки (Obsidianize /
  Чат-обработка / AI-анализ). Иконка рядом с «?» следует за текущей вкладкой;
  окно закрывается крестиком, кликом по фону или Esc и перетаскивается.
- Вкладка Obsidianize получила кристалл Obsidian (inline SVG, фиолетовый
  градиент) вместо обычной папки; значки чата 💬 и робота 🤖 сохранены.

Первый minor-релиз: линейка 0.3.x (восстановление → исправления →
документация) завершена, разработка фич продолжается на 0.4.x.

## [0.3.9] — 28.08.2026

### Добавлено
- Перетаскиваемая граница между таблицей скана Obsidianize и журналом
  обработки: при сотнях карточек таблица больше не уводит результаты за
  экран — потяните границу вверх/вниз, чтобы изменить размер области
  карточек (от 120 px до 80% высоты окна); выбранная высота сохраняется
  между запусками GUI.
- Таблица скана прокручивается внутри своей области, и заголовок остаётся
  видимым (закреплённый заголовок) при прокрутке длинных списков.

## [0.3.8] — 28.08.2026

### Релиз документации
- README (RU+EN): новый подраздел «Частые ситуации» в Folder Obsidianizer —
  ответы простым языком (без кода и флагов) на вопросы, которые возникают у
  каждого нового пользователя: папка без карточки, случайная заметка,
  созданная самим Obsidian (Ctrl+клик), и как «принятие» превращает её в
  файл заметок; обещание «заметки никогда не перезаписываются»; честная
  пометка «требует обновления» после правки полей; отсутствие принуждения к
  немедленному обновлению; ноль записей в файлы до явного запуска.
- `docs/obsidianize.md` + `.ru.md`: те же сценарии встроены в разделы
  сценария работы с реальным словарём статусов (missing / conflict / stale /
  ok).

## [0.3.7] — 28.08.2026

### Релиз документации
- README (RU+EN): новые разделы «Folder Obsidianizer» и «Интеграция с
  Obsidian (хоткей Templater)» — что такое карточки проектов, где живёт
  пользовательский слой `_заметки.md`, настройка хоткея за 4 шага (папка
  шаблонов, путь к `cli.bat`, горячая клавиша, Notice-самодиагностика), блок
  CLI `folders` со всеми флагами и описание трёх вкладок GUI (Obsidianize /
  Чат-обработка / AI-анализ).
- `docs/obsidianize.md` + `.ru.md` полностью переписаны под текущую карточку
  RENDER_VERSION=8 (семантика Folders / Files / About / Gallery / Images /
  AI Review / Notes, экранированная строка `⬆ Up`, правила Gallery vs
  Images, сортировка таблицы файлов), сценарий работы с нуля, пути обновления
  через Templater и Shell commands, техническое приложение (манифест с
  `base`, базис папки карточки, детерминированное выравнивание старых
  манифестов, агрегаты `--no-recursive`, diff'ы скана в `obsidianizer.log`).
- `obsidian/obsidianizer-refresh.md` актуализирована: `--rel` в обоих
  вариантах хоткея, Notice-самодиагностика, явная инструкция «замените путь к
  cli», обновлённый список секций карточки.
- `AGENTS.md`: раздел Folder Obsidianizer синхронизирован с текущим рендером
  (структура v5, RENDER_VERSION = 8, базисонезависимость, агрегаты
  ограниченного обновления, покрытие тестами).

Резюме релиза: v0.3.4 починил сломанную строку `⬆ Up` в таблице, v0.3.5
устранил рассинхрон базисов Scanner ↔ Templater (больше нет фантомных
«добавлен/удалён»), v0.3.6 починил обнуление таблицы Folders при
`--no-recursive`. 12 карточек тестового хранилища пересобраны и проверены
(все `version: 8`, манифесты с `base`, строки Up — 4 ячейки, нулевых
агрегатов нет).

## [0.3.6] — 28.08.2026

### Исправлено
- `--no-recursive` (хоткей Templater) обнулял таблицу Folders: дерево
  обрезалось до корневой записи ДО `folder_stats`, поэтому каждая строка
  подпапки рендерилась как `0 / 0 B / пусто`, тогда как GUI-запуск
  (рекурсивный) показывал реальные значения. Теперь статистика считается по
  полному поддереву; обрезка ограничивает только цикл записи карточек.
  Регрессионный тест фиксирует реальные агрегаты и контракт «записывается
  только карточка корня».

## [0.3.5] — 28.08.2026

### Исправлено
- Рассинхрон базисов Scanner ↔ Templater: ключи файлов в манифесте и
  fingerprint папки молча зависели от корня скана. Карточка, обновлённая из
  своей папки (хоткей Templater) и просканированная из общего корня проекта,
  показывала «добавлен/удалён» для каждого файла и вечный статус
  «требует обновления». Исправление — обе стороны сравниваются в базисе
  папки карточки:
  - `folder_fingerprint` строит пути относительно папки карточки
    (базисонезависим; существующие карточки один раз станут stale и
    перегенерируются при следующем обновлении);
  - `_manifest_payload` пишет ключи относительно папки карточки плюс поле
    `base` (rel карточки от корня обновления, для диагностики);
  - `card_diff` выравнивает старые манифесты (без `base`) детерминированным
    правилом префикса: если хотя бы один сохранённый ключ несёт префикс
    корня скана, он отрезается у всех ключей — без подсчёта пересечений.
- `_MANIFEST_RE` получил `re.DOTALL`: многострочный JSON больше не может
  молча сломать разбор манифеста (защита; текущие записи однострочные).

### Добавлено
- `obs_scan` пишет в `obsidianizer.log` diff каждой устаревшей карточки
  (корень скана, rel папки, счётчики added/removed/changed) для разбора
  post-mortem.

## [0.3.4] — 27.08.2026

### Исправлено
- Строка «⬆ Up» в таблице Folders содержала сырой alias-пайп
  (`[[../X|Up]]`), который Markdown-парсер таблицы считал разделителем
  ячеек — строка распадалась на 6 «ячеек» и ломала таблицу. Пайп теперь
  экранирован (`[[../X\|Up]]`), как в строках папок; таблица сохраняет
  ровно 4 ячейки. Тест дополнен структурной проверкой числа ячеек;
  корневая карточка (без строки Up) покрыта существующим тестом.

## [0.3.3] — 27.08.2026

### Добавлено
- UI вкладки Obsidianizer (v5.12): поле «Базовый путь галереи (для проектов
  вне Vault)» с сохранением, чекбокс «Принять существующую заметку как
  заметки» (adopt), строки ⚠-изменений под устаревшими папками в таблице
  сканирования (из `changes` в `obs_scan`), подсказка для чужих заметок,
  доступных к принятию, и однострочная сводка сканирования в статус-баре.
  Значения восстанавливаются из `defaults()` и сохраняются при изменении.

## [0.3.2] — 27.08.2026

### Добавлено
- Backend вкладки Obsidianizer для паритета с v5.12: автоматическое
  определение корня vault (`_find_vault_root` поднимается до `.obsidian`),
  когда не заданы ни vault_root, ни gallery_prefix; метод бриджа
  `set_obsidianize_gallery_prefix`; `obs_obsidianize` передаёт `adopt` и
  `gallery_prefix`.
- Умное сканирование (`obs_scan`): по каждой папке список `changes` из
  скрытого манифеста карточки (`card_diff` / `format_changes` — добавлены /
  удалены / изменены файлы, структура папок, данные проекта), флаг
  `adoptable` для чужих заметок без файла заметок и однострочная сводка
  (проверено / без изменений / требуют обновления).

## [0.3.1] — 27.08.2026

### Добавлено
- Флаги `obsidianizer folders`: `--adopt`, `--gallery-prefix`, `--rel`
  (инфраструктура v5.12): принятие чужой заметки в `<имя>_заметки.md`
  (переименование 1:1), запасной базовый путь галереи для блока
  `img-gallery`, когда проект вне vault, и vault-relative путь корня для
  ссылки «⬆ Вверх» при локальном обновлении одной карточки. Разблокирует
  хоткей Templater в Obsidian, который уже передаёт эти флаги.

## [0.3.0] — 27.08.2026

### Добавлено
- Стабильная точка восстановления после катастрофического `git stash` +
  `reset --hard` (вернулись все 3 вкладки, 288 тестов зелёные). Ядро карточек
  (`obsidianize.py`) содержит структуру v5.11/v5.12 с `RENDER_VERSION = 8`
  (Folders / Files / About / Gallery / Images / Notes).
- Страховочные ссылки восстановления: ветки `recovery-pre-v5.11`
  (состояние до v5.11/v5.12) и `recovery-current-v5.11` (состояние с
  CLI/UI-частями v5.11/v5.12), плюс `backup-before-recovery` на базовом
  коммите; тег `v0.3.0`.
- Двухэтапный вывод: `processed/` (импорт) и `enriched/` (импорт + LLM).
- Постоянные настройки GUI: `config.yml` — единое хранилище путей, модели,
  переключателя Ollama и флагов prune / dry-run. `Settings.save()` сливает
  данные в существующий файл и пишет атомарно; GUI сохраняет при каждом
  подтверждённом изменении, перед запуском и при закрытии окна, поэтому
  повторно открытое окно восстанавливает ровно то состояние, в котором было
  закрыто. Каждая кнопка `Выбрать` открывает проводник в своём последнем
  каталоге с откатом на ближайший существующий родитель.
- Переключатель AI-постобработки: чекбокс «Использовать Ollama» стал
  «AI-постобработка» — Ollama трактуется как деталь реализации AI-слоя. При
  выключенном переключателе настройки модели и промпта скрыты в
  раскрывающемся блоке `#aiDetails`; при включении появляются комбо-бокс
  модели и кнопка `Изменить…`, а также серая подпись «Локальная модель через
  Ollama». Кнопки действий получили подписи: «ОБРАБОТАТЬ» показывает
  «Импорт + AI» / «Только импорт», вторичная кнопка AI — «Только повторный
  AI-прогон».
- Выбор модели и редактор промптов за переключателем AI: поле модели —
  комбо-бокс (свободный ввод + выпадающий список установленных моделей
  Ollama; свой список, а не `datalist`, поэтому каждая модель видна независимо
  от текста в поле; `list_models`, обновляется по требованию через
  `GET /api/tags`; только имена, текущая модель всегда сохраняется,
  недоступная Ollama показывает статус вместо падения UI). Кнопка `Изменить…`
  открывает модальный редактор с вкладками импорт / AI, с `set_prompt` /
  `reset_prompt` для каждого промпта (встроенные `DEFAULT_PROMPT` /
  `DEFAULT_AI_PROMPT` не перезаписываются; `{content}` остаётся единственной
  точкой подстановки).
- AI-этап (`obsidianizer ai`): читает заметки processed и пишет enriched-vault;
  инкрементален через маркер `ai_hash` — заметка пропускается, когда её
  `ai_hash` совпадает с `source_hash` из processed.
- Опции `--enriched DIR` / `--prune-enriched`; очистка сирот маркеро-зависима
  (только файлы с `ai_hash`, чьей пары нет в processed, и медиа, на которые
  ничего не ссылается).
- Гарантия разделения: пары папок соседних этапов не должны пересекаться
  (`(source, target)`, `(target, enriched)`, `(source, enriched)`).
- События AI-этапа: `AI_SCAN_STARTED`, `AI_FILE_STARTED`, `AI_FILE_DONE`,
  `AI_FILE_SKIPPED`, `AI_FILE_ERROR`, `AI_FINISHED` (заменяют старый
  `LLM_STARTED`).
- Объединение в тему («Объединить в тему…»): выбор нескольких чатов processed
  в модальном окне (чекбоксы + поиск) и слияние их в одну тему
  `enriched/topics/<Имя>.md`. Один вызов LLM даёт
  NAME / SUMMARY / DECISIONS / KEY_FACTS / ARTIFACTS и источники по сервисам
  со ссылками на исходные чаты; имя файла берётся из блока NAME модели
  (очищается от спецсимволов). Инкрементально через `topic_hash` (хэш
  `source_hash` выбранных чатов): неизменный набор пропускает вызов LLM.
  Новый шаблон `topic_prompt` (третья вкладка редактора промптов), события
  `TOPIC_*` и методы моста `list_chats()` / `create_topic()`.
- «ОБРАБОТАТЬ» теперь чистый импорт: AI-этап автоматически не запускается.
  AI работает только по явному действию — кнопка «AI-постобработка» (по
  каждому чату) или «Объединить в тему…». Подпись кнопки зафиксирована как
  «Только импорт».
- Авто-группировка («Авто-группировка»): все чаты processed автоматически
  группируются в темы без ручного выбора. Проход `map_prompt` кластеризует
  карточки чатов (название/источник/дата + фрагмент тела) в темы через блоки
  `TOPIC:`/`IDS:`; большие коллекции разбиваются на чанки по размеру payload,
  а уже найденные имена тем передаются дальше, чтобы одна тема сохраняла имя.
  Каждый кластер из двух и более чатов проходит тот же путь `create_topic`
  (инкрементально через `topic_hash`); кластеры из одного чата пропускаются,
  существующие темы никогда не удаляются. Новый шаблон `map_prompt`
  (четвёртая вкладка редактора промптов), событие `TOPIC_MAP_STARTED` и
  метод моста `group_all()`.

## [0.3.0] — Не выпущен

### Добавлено
- Десктопный GUI (`obsidianizer ui`, pywebview): выбор источника/результата,
  переключатель Ollama + модель, опции dry-run/prune, прогресс-бар, текущий
  файл, цветной журнал, итог, кнопка «Открыть результат». Чистый фронтенд
  поверх того же ядра.
- Честная остановка: `pipeline.run` получает `cancel_check` (опрос только
  между файлами; текущий файл — включая долгий вызов Ollama — доводится до
  конца).
- Гарантированный одиночный `FINISHED` на любом пути (обычное завершение,
  ошибка файла, отмена, фатальная ошибка запуска). `Report.cancelled` — только
  при пользовательской отмене; `Report.critical_error` фиксирует фатальные
  ошибки.
- Корневой лаунчер `Obsidianizer.py` / `Obsidianizer.bat`: двойной клик
  запускает GUI прямо из корня репозитория — установка пакета не нужна;
  `Obsidianizer.py --check` выводит диагностику без открытия окна.

## [0.2.0] — Не выпущен

### Добавлено
- Двуязычная документация (RU + EN): `README.ru.md`, `AGENTS.ru.md`,
  `CHANGELOG.ru.md`, `docs/*.ru.md`; политика двуязычности в `AGENTS.md`.
- Контракт событий (`events.py`): `SCAN_STARTED`, `FILE_STARTED`,
  `LLM_STARTED`, `FILE_DONE`, `FILE_SKIPPED`, `FILE_ERROR`, `FINISHED`.
  `pipeline.run` принимает необязательный колбэк `on_event`; ядро остаётся
  независимым от способа отображения.
- Плейсхолдер будущего GUI (`ui.py`) и каталог `assets/`.
- Архитектурная документация: раздел «Слой UI» с протоколом событий.

## [0.1.0] — Не выпущен

### Добавлено
- Каркас репозитория: src layout, pyproject, тесты, документация, контракт AGENTS.
- Ядро конвейера: scan → extract → enrich → emit.
- Безопасность: guard (запрет пересечения source/target), манифест владения,
  атомарная запись.
- Процессор Markdown для сырых экспортов чатов ИИ (извлечение метаданных +
  резюме/теги через Ollama, корректная деградация при недоступной LLM).
- Инкрементная обработка через `source_hash` в frontmatter результата.
- Генерация `_index.md` для навигации по vault.